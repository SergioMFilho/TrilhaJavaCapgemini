import React from 'react';
import styled from 'styled-components';

const Title = styled.h1`
color: blue;
font-size: 20px;
`;

const Menubar = () => (
    <menubar>
        <Menubar>EAD Proway</Menubar>
    </menubar>
);

export default Menubar;